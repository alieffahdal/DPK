#include <stdio.h>

int main() {
    int sum;

    // inisialisasi nilai sum
    sum = 65;

    // cek kondisi dengan if
    if (sum < 65) {
        printf("Maaf, anda harus mencoba lagi\n");
    } else {
        printf("Selamat, nilai anda cukup!\n");
    }

    return 0;
}
